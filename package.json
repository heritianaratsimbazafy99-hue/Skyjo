{
  "name": "skyjo-score-arena",
  "version": "1.0.0",
  "private": true,
  "description": "Plateforme visuelle pour noter les scores de Skyjo avec QR mobile synchronise.",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "check": "node --check app.js && node --check server.js && node --check chaos-engine.js",
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "qrcode": "^1.5.4"
  }
}
